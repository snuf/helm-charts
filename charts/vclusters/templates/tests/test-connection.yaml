apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vcluster-ingress.fullname" . }}-test-connection"
  labels:
    {{- include "vcluster-ingress.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vcluster-ingress.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
