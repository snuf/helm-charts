apiVersion: v1
kind: Pod
metadata:
  name: "{{ .Values.host.name  }}-test-connection"
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vcluster.internalName" . }}:443']
  restartPolicy: Never
